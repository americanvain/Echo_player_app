# Changelog

## v0.3.0 — 2026-09-05

内置离线词典，听读页主区域充实。

- **离线词典**：随 APK 内置 7.3 万词（音标、释义、词形变化），由 MIT 协议的 ECDICT 筛选生成。
  点词直接出释义和音标，不再需要服务器或点放大镜；"blew" 之类的屈折形式会连原形一起给。
- **本句词汇**：每句自动列出超出常用词范围的词，一行一个加简短释义，点它等于点句子里的那个词。
- **问 AI**：听读页底部新增问答区，快捷问题 + 输入框，问答挂在句子上保存。
  服务器接 `/chat`（教学 Agent）；离线时本机能答某个词的意思、这句怎么读（逐词音标）、难词、整句意思。
- **盲听修正**：被盖住的词选中时色块变成主题色，能看出选了哪几个；选区栏、底部提示和定位面板
  都不再露出被盖住的原文，面板里可以「看一眼原文」再盖上。
- 数据库 v3（自动迁移），新增 `chat_messages`。APK 从 1.9 MB 增至 4.4 MB（词典压缩后 2.5 MB）。


## v0.2.1 — 2026-09-04

听读页的交互返工。

- **点词、划词重写**。整句改成一个文本，用文字布局做命中测试，手势只有一处，自己分开
  点击 / 长按划选 / 滚动三种情况。原先每个词是一个可点击组件、外面再套长按拖动检测器：
  子组件会把长按吃掉导致划不动，长按选中后抬手还会补一次点击把选区清掉，几十个可点击
  组件也拖慢了滑动。现在划选跟手，滚动顺畅。
- **点词直接出释义**，不再弹选项列表。就地展开一张小卡片：释义 + 一排小图标（朗读、
  加入生词本、查词典、复制、关闭）。释义优先取生词本里已记的，其次问服务器 `/translate`，
  没有服务器时提示并保留词典入口。
- **盲听模式改成盖词**。句子照常排版，每个词盖一个色块，点一个揭开一个，也可以「全部显示」。
  盖住时照样能长按划选、标记问题，译文在全部揭开前不显示。
- **去掉右上角菜单**。盲听改成标题栏的一个图标，翻译开关移到底部按钮行，其余功能底部本来就有。
- **上文排得更松**：行距加大、颜色更淡、左侧一条竖线，和当前句拉开距离；点一句可以跳回去。


## v0.2.0 — 2026-09-04

问题定位做细，新增练习区。

**问题定位精确到可出题**
- 在句子上**长按并划过**即可选出卡住的那几个词；选中后可只听这一段、直接加入生词本
- 每一层给出 6~7 个**细分类型**（连读 / 弱读吞音 / 语速 / 相似音混淆 / 熟词生义 / 从句嵌套 / 言外之意…），多选；再选**程度**（看原文才懂 / 听出词没懂 / 完全没听出）
- 词形层可以填"听成了什么"，它会直接变成练习里的干扰项
- 一条记录 = 句子 + 词范围 + 层 + 细分类型 + 程度 + 可选的一句话，每个细分类型都带英文描述原样发给 AI
- 记录页的问题卡片显示划选片段与细分标签

**练习区（新的底部标签）**
- 「生成练习」把未解决的问题、待复习的生词、跟读评分交给服务器 AI 分析出题；没有服务器时用本机规则从同一份记录出题，结构完全一致
- 八种题型：闪卡、选词填空、听写填空、句子重组、辨音对、跟读评分、选译文、讲解
- 辨音题由跟读评分里的音素错误驱动，内含 29 组中国学习者高频混淆音的最小对立词
- 进度可中断续做；一组做完后答对的题所对应的问题记录自动标记为已解决，结果回传服务器

**结构调整**
- 底部导航改为 书架 / 练习 / 记录 / 设置；生词本并入「记录」页的一个标签（生词本身就是词义层的问题记录）
- 练习和记录标签带角标，显示未完成的练习集与待解决的问题数
- 数据库升到 v2（自动迁移，不丢数据）


## v0.1.0 — 2026-09-04

首个版本。

- 书架：10 篇内置资源（A1~C1，含中文翻译），导入 TXT（本机切句）、PDF（交给服务器）、粘贴文本；系统「分享 / 打开方式」可直接送进书架
- 听读：上下文 + 当前句、点词操作、翻译开关、盲听模式、倍速、单句循环、自动连播、目录跳转
- 跟读：16 kHz WAV 录音 → speecheval `/assess` 逐音素评分；每词得分、逐音素判定、回放自己读的每个词；历史尝试
- 五层问题定位：语音 / 词形 / 词义 / 句法 / 语义，每层的定义、典型表现、可做动作、疑问记录、AI 讲解（离线模板兜底）
- 生词本：原句与译文、熟悉度、释义与笔记、复习卡片
- 记录：问题时间线（按层筛选、已解决）、跟读评分时间线、五层分布
- 设置：服务器地址与连通性测试、TTS 语速与试听、听读默认项
